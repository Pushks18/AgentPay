[package]
name = "sp1-reputation-program"
version = "0.1.0"
edition = "2021"

# SP1 guest programs must use nightly: `rustup override set nightly` in this dir
[dependencies]
sp1-zkvm = "3.0.0"
sha2 = { version = "0.10", default-features = false }
serde = { version = "1.0", features = ["derive"] }
